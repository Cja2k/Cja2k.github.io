The object diagram can be changed when one object has or requires other objects to operate properly. It is changed by creating the main object and the secondary object, and having the first object reference the secondary one.